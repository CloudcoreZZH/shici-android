# 局部校正

以下仍使用内置 image_gen，输入为对应方案的初稿图片；只修正造成交互误解的局部内容。

## B：去掉无关天气信息

```text
Edit this existing design board with ONE tightly localized correction. In the upper right of screen 01 (the upper-left phone artboard), directly below the date, remove the sun/weather icon and the text 晴 26°C. Replace that small weather row with the simple muted-blue label 离线学习 and no icon. This is an offline vocabulary app, not a weather app. Preserve absolutely everything else: the six-screen grid, all screen geometry, typography, all other text, colors, controls, illustrations, overall image dimensions and the title B · 原生晴空. No other redesign and no additional elements.
```

## D：修正“忘记”图标与进度总数

```text
Make one targeted semantic correction on this exact existing D · 夜航专注 design board, preserving the six-screen layout, palette, all typography, all screen positions and every other UI detail. On screen 04 揭晓评分 (bottom-left artboard), the bottom red 忘记 1天 rating button incorrectly has a trash-bin icon. Replace ONLY that trash-bin icon with a simple thin red counterclockwise circular-arrow icon meaning try again/relearn. Forgetting a word must never look like deleting a word. The label 忘记 and interval 1天 remain exactly the same. Also on screen 01 top-left home progress circle, change its small denominator / 36 to / 44, since completed 8 + remaining learning 12 + remaining review 24 = 44. Keep all other numbers, lines, buttons, text, icons and design elements unchanged. Preserve the same output size and overall composition.
```
